name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Test
      run: |
        echo $(find . -type f -print0 | sort -z | xargs -0 sha1sum | sha1sum)
        find . -type f -print0 | sort -z | xargs -0 sha1sum | sha1sum
        
    

